class example [
	fields temp

	method ifStatement(a, b) with locals c:
		if (a > 5): {
			c = a
		} else {
			c = b
		}
		return c
]

main with x:
	x = @example
	!x.temp = 3
	print(^x.ifStatement(0, 3))